# Public site URL for canonical links, sitemap, robots, and JSON-LD (no trailing slash).
# Required for production builds (e.g. on Vercel project settings → Environment Variables).
NEXT_PUBLIC_SITE_URL=
